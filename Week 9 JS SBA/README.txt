This web page is a starter page for my eCommerce site. It has a main (index) landing page, a login page, a signup page, and a cart page (purely nonfunctional - has an example table). The menu on the left is also nonfunctional as of yet. Icons at the top take you to the login page and cart. Signup page performs validation beyond the HTML validation for the inputs.


Technical requirements:

HTML

* 4 pgs *	Have at least 3 pages, keep the grid system consistent as much as possible
* 12+ tags *	Use at least 10 different HTML tags
* Cart Page *	Use HTML tables
* Login/Signup*	Implement at least two uses for forms
* Signup Page *	Dropped Down Menu 
* All Pages*	Use web fonts
* All pages*	Use different types of content in the form of text, images, videos, and GIFs
Use regex validation	*signup.html Line 136 email validation*


CSS

Inline, internal, and external styling	*inline on body tag*
Use five different CSS selectors	*tag, id, class, composite*
Don’t use too many fonts		*3 fonts used*
Use colors that complement each other	*I think so. Kind of a bluish scheme*
Use Flexbox (Optional)			*some flex in bootstrap. Not sure if that counts*
Use SASS/SCSS (Optional)
Use animations (Optional)		*hamburger menu - NOT COMPLETED*


Javascript

External scripts *script.js*
Use variables, *ok*
if statements, *validation function*
loops, *forEach to add states to dropdown menu*
at least one form of collections, *array of state abbreviations*
functions/call back, *validateForm()*
and events *onsubmit()*
Use AJAX (Optional) 
Use JSON or XML (Optional)
Use JQuery (Optional)